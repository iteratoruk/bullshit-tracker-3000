[tool.poetry]
name = "bullshit-tracker-3000"
version = "0.1.0"
description = "A persistent desktop timer that tracks how much of your life is wasted by workplace nonsense."
authors = ["Your Name <you@example.com>"]
license = "MIT"
readme = "README.md"
packages = [{ include = "bst3000" }]

[tool.poetry.dependencies]
python = "^3.10"
tk = "*"

[tool.poetry.scripts]
bst3000 = "bst3000.main:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
